#include "main.h"

/**
 * _islower - function that checks for lowercase character
 *
 * @c: variable number of the caracter in ASCII
 *
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	if (c > 93)
		return (1);

	return (0);
}
